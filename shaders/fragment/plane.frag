#version 460 core

out vec4 FragColor;


void main()
{
    //float h = (Height + 16)/32.0f;	// shift and scale the height into a grayscale value
    //vec3 res = Height;
    //res = normalize(res);
    //res.y = Height.y * -1;
    FragColor = vec4(1.0);
}